public interface Info {
    // interfaces have no code so no curly brackets. only header of methods
    // could have any number of methods, return types, parameters, names
    public void showInfo();
}